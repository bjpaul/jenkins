pipeline{
    agent{
        docker{
            image 'gcr.io/kaniko-project/executor:v1.6.0-debug'
            args  '--entrypoint sleep123'
        }
    }
    stages{
        stage("Init"){
            steps{
                sh "30s"
            }
        }
    }
}
