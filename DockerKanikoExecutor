pipeline{
    agent{
        docker{
            image 'gcr.io/kaniko-project/executor:debug'
            args  '-it --entrypoint /busybox/cat'
        }
    }
    stages{
        stage("Init"){
            steps{
                sh "30s"
            }
        }
    }
}
