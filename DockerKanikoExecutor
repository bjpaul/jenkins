pipeline{
    agent{
        dockerfile{
            dir 'build'
            filename 'DockerKanikoExecutor'
            additionalBuildArgs  '--build-arg sleeptime=30m'
        }
    }
    stages{
        stage("Init"){
            steps{
                sh "cat /kaniko/executor"
            }
        }
    }
}
